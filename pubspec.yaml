name: reasonix_mobile
description: Reasonix AI coding assistant for mobile
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.0
  path_provider: ^2.1.0
  http: ^1.6.0
  url_launcher: ^6.3.0
  intl: ^0.20.0
  file_picker: ^8.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
